export class User{//es una interfaz de los datos del usuario, no afecta si no vienen todos los datos
    id:number;
    nombre: string;
    email: string;
    red: string;
    comunidad: string;
    pago: string;
    folio: string;
}